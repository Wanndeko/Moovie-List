import { styled } from 'styled-components'

export const Container = styled.div`
  color: #fff;
  padding: 0 20px;
  overflow-x: hidden;

  h2 {
    margin: 50px 0 20px 20px;
    font-size: 24px;
  }

  .swiper-wrapper {
    display: flex;
  }
`
